#include "main.h"
/**
 * mul - Entry point
 * @a: the first parameter
 * @b: the second parameter
 *
 * the function will multiply the two parameters
 *
 * Return: the result of multiplication
 *
 */
int mul(int a, int b)
{
	return (a * b);
}
